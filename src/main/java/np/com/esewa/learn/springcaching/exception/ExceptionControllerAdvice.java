package np.com.esewa.learn.springcaching.exception;

//import org.springframework.http.HttpStatus;
//import org.springframework.http.ResponseEntity;
//import org.springframework.web.bind.annotation.ControllerAdvice;
//import org.springframework.web.bind.annotation.ExceptionHandler;

//@ControllerAdvice // allows to handle exceptions accross whole application
// class for custom exceptions, instead using StudentNotFoundException
// this class is not in action yet, need time to take it in action
public class ExceptionControllerAdvice {
   /*
    // exception for student not found
    @ExceptionHandler
    public ResponseEntity<Object> handleStudentNotFoundException(Exception e){
        String errorMessage = "Student Records not found";
        return new ResponseEntity<>(errorMessage, HttpStatus.BAD_REQUEST);
    }
    */
    // custom exception for resource not found.

}
